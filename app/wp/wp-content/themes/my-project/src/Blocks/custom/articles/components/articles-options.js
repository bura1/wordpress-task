import React from 'react';
import { __ } from '@wordpress/i18n';
import { PanelBody } from '@wordpress/components';

export const ArticlesOptions = () => {
	return (
		<PanelBody title={__('Articles', 'my-project')}>
		</PanelBody>
	);
};
